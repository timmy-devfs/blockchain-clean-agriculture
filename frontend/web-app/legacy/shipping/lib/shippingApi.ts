/**
 * Re-export — module thật nằm tại `app/lib/shippingApi.ts` (dashboard import `../lib` từ `app/dashboard`).
 * Giữ path này để tài liệu / import cũ `legacy/shipping/lib/shippingApi` vẫn resolve.
 */
export * from "../app/lib/shippingApi";
